import { createStore } from 'vuex'
import axios from 'axios';
const state = {
  cartItems: []
}

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
